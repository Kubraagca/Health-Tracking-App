name: saglik_takip
description: Günlük sağlık aktivitelerini takip eden bir uygulama.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
  charts_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
